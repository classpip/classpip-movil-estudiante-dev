import { FiltrarAlumnoEvaluadorPipe } from './filtrar-alumno-evaluador.pipe';

describe('FiltrarAlumnoEvaluadorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarAlumnoEvaluadorPipe();
    expect(pipe).toBeTruthy();
  });
});
