import { FiltrarComentariosVaciosPipe } from './filtrar-comentarios-vacios.pipe';

describe('FiltrarComentariosVaciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarComentariosVaciosPipe();
    expect(pipe).toBeTruthy();
  });
});
