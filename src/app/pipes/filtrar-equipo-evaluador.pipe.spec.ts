import { FiltrarEquipoEvaluadorPipe } from './filtrar-equipo-evaluador.pipe';

describe('FiltrarEquipoEvaluadorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarEquipoEvaluadorPipe();
    expect(pipe).toBeTruthy();
  });
});
